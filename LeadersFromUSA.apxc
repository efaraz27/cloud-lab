public class LeadersFromUSA {
    public void printLeadersFromUSA(){
        List<Lead> leads = [SELECT Country FROM Lead WHERE Country IN ('USA')];
        System.debug('Number of leaders in USA: '+ leads.size());
    }
}
