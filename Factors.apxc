public class Factors {
    public void factors(integer num){
        System.debug('Factors are: ');
        for(integer i =1 ; i <= num; i++){
            if(Math.mod(num, i) == 0)
                System.debug(i);
        }
    }
}
